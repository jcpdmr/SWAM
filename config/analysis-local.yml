spring:
  main:
    web-application-type: none

  devtools:
    livereload:
      enabled: false

  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: secret
    in-queue: analysis_in
# logging:
#   level:
#     org.springframework.boot.devtools: DEBUG
